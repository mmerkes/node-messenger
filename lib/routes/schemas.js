'use strict';

module.exports = function (app) {

};

// POST /schemas
// Create a new schema

// GET /schemas
// Get all schemas in DB

// GET /schemas/:schema
// Get a specific schema in the DB

// DELETE /schemas/:schema
// Delete a specific schema in the DB